#!/bin/bash
#This is a simple script, but I am too lazy to change it as I dont need more flexibility
ln -sf ~/dotfiles/vimrc ~/.vimrc
ln -sf ~/dotfiles/zshrc ~/.zshrc
ln -sf ~/dotfiles/prompt.sh ~/.prompt.sh
ln -sf ~/dotfiles/tmux.conf ~/.tmux.conf
